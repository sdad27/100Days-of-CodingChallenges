// Extracting a Substring: Demonstrate how to extract the first 10 characters from a string.

function checks(sentence:string) {
    return sentence.substring(0,10);
}
console.log(checks("learn code with javascript"));

